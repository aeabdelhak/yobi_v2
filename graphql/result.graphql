type userResult {
    id: ID!
    file: file
    landing: landingPage
}

extend type Mutation @guard {
    deleteUserResult(id: ID!): userResult @delete
}
extend type Query @guard {
    userResult(id: ID! @eq): userResult! @find
    usersResults: [userResult] @all
}
